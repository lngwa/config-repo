server:
  port: 8100

spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: cur_app
    password: anything
    url: jdbc:mysql://localhost:3306/cur_proj?createDatabaseIfNotExist=true